# Ignore large PDF source files to prevent Git bloat and stay under GitHub 100MB file limit
cikmis_sorular/*.pdf
*.pdf

# System & OS files
.DS_Store
Thumbs.db
test_page*.png

# Sensitive data — local backups only, must NOT go to GitHub
# Ilay's real data lives in Firebase. These are local safety copies.
archive/backups/
archive/logs/
archive/docs/
backup_*.json
current_firebase_data*.json
*_SAFE_COPY_*.json

# Tesseract local models
cikmis_sorular/tessdata/
*.traineddata

# Python cache & temporary files
__pycache__/
*.pyc
scratch_*.txt
